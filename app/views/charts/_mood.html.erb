<script> 
$(function () {
        $('#container1').highcharts({
            title: {
                text: 'Mood - Analytics',
                x: -20 //center
            },
            xAxis: { 
	
                categories: [ 
                              <% @mood_data.each do |t| %> 
                                '<%= t.entry_at.strftime("%m/%d/%y") %>',  
                              <% end %>
                            ]
            },
            yAxis: {
                categories: ['0', 'Mild', 'Moderate', 'Severe'],
                lineColor: '#FF0000',
								lineWidth: 1,
								min: 0, 
								max: 3
            },
            tooltip: {
              
            },
            legend: {
                layout: 'vertical',
                align: 'right',
                verticalAlign: 'middle',
                borderWidth: 0
            },
            series: [{
                data: <%= @mood_data.map{|vv| vv.mood }%>
            }
          ]
        });
    });  

</script> 
    
